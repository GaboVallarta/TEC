"""
A cell phone company charges $0.80 per message, per mega or per minute. Make a program that calculates the total monthly cost of a user based on these data.

Input:

The number of messages (integer), the number of megabytes (float number), and the number of minutes (integer). One data per line and in that order.

Output:

A number that represents the monthly cost."""

snail=float(5.7)

minutes=float(input("Enter the minutes: "))
seconds=float(minutes * 6)

print(f"The final distance travel is about { seconds* snail} cm")